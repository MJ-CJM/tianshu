# 决策与审批记录（D1–D8）

> D1–D7 是 2026-07-12 的基线裁决；D8 是 2026-07-14 的当前范围裁决。
> D8-A 取代 D4-A 作为当前交付范围，但不删除完整 G0–G5 长期路线。

## D1 资产回收策略

| 选项 | 内容 | 后果 |
|---|---|---|
| **A. 全量快进迁移（推荐）** | 按 [00 号文档](./00-baseline-and-recovery.md) 把 44 个提交 + WIP 冻结分支迁入当前分支 | 零冲突（merge-base 已验证）；保留全部已审查工作与提交历史 |
| B. 只迁文档、代码重做 | 丢弃 feat_codex_phase_1 的实现 | 重做约 2.7 万行经多轮审查的工作，无收益；不推荐 |
| C. 回源 clone 继续 | 开发回到 ~/tiangong/tianshu | 双现场漂移风险持续；与当前工具链/会话割裂 |

## D2 唯一开发现场

**推荐**：迁移后本工作区（`tianshu-worktree/tianshu`）为唯一现场；
`~/tiangong/tianshu` 按 P0.7 封存为只读应急副本。

## D3 备份方式（涉及一次外部动作授权）

| 选项 | 内容 |
|---|---|
| **A. bundle + 推送 origin（推荐）** | 本地 bundle 之外，将 `feat_codex_phase_1` 与 `wip/g1.4b3-freeze` 推到私有 origin 作双保险。`git push` 属外部状态变更，需要你在此项明确授权（仓库当前为私有，不涉及公开发布） |
| B. 仅本地 bundle | 不动远程；备份仍在同一台机器上，抗灾等级较低 |

## D4 原实施范围（2026-07-12 历史裁决，当前由 D8 覆盖）

| 选项 | 内容 | 量级 |
|---|---|---|
| **A. 完整 G0–G5** | [01 号计划](./01-master-plan.md) 当时的 56 个剩余切片 | 当时粗估 1–2 周 agent 连续工作 + 审批/外部窗口 |
| B. 核心优先 | S0 全部；S1 仅 Doctor（S1.4）；S2 仅审计+MCP 密文（S2.1–S2.3）；S3 全部；S4 壳层+三真实页+质量门（S4.1–S4.7、S4.12）；S5 前半（S5.1–S5.5）；S6 延后 | 约减 4 成；"可治理、可验证、真实自进化路由"三支柱不受影响；"可安装发布"能力延后 |

两档均保留逐 Gate 审批；选 B 后随时可升级回 A（计划结构不变）。

## D5 Gate 审批节奏（2026-07-12 历史裁决，经 D8 收窄）

| 选项 | 内容 |
|---|---|
| **A. 逐 Gate 审批（推荐）** | 每个阶段出口交报告，你批准后进下一阶段（审批点见 01 号计划第 12 节）。你上次叫停连续实施，本项即对应的替代授权模式 |
| B. 连续实施至 G5 | 恢复 codex 原授权：Gate 间不等待人工审批，自动验收，最终统一交付（S4 视觉终审与 S6 发布授权仍单独保留） |

## D6 文档权威关系（默认执行项）

`cc-fable-v1` 为执行权威（顺序/状态/审批），`codex-v1` 为技术与历史事实源，
其历史文档不做修改；当前 staged 的 `docs/codex-v1` 副本在 P0.3 丢弃，
以分支内已提交版本（`7386cf3`，字节级一致已验证）为准。

## D7 仓库卫生小项（默认执行项）

`.idea/*.iml` 等 IDE 文件仍被跟踪（`.gitignore` 目前只忽略 workspace.xml/
modules.xml）。**推荐**：S0 收口后以独立小提交停止跟踪（`git rm --cached` +
gitignore 补条目），不与任何功能提交混合；若你希望严格按原计划留到 G5
仓库卫生阶段（S6.8），改选"延后"即可。

## D8 当前交付范围（2026-07-14）

| 选项 | 内容 | 结果 |
|---|---|---|
| **A. 精简 Developer Preview（已批准）** | 完成 S1/G1.5 Gate；S2 审计/密文/最小 fail-closed；S3 持久治理与 Evidence Core；S4 壳层+三真实页+精简质量门；S5 候选/门禁/晋升/真实分流 Core；一个黄金 Demo 与最小开源候选包 | 优先证明“可治理、可验证、会成长、会进化”的独有竞争力；保留 Wheel CI，延期容器、公开注册表、全量部门、完整执行器中立与供应链工程 |
| B. 恢复完整 G0–G5 | 重新启用 D4-A 的所有剩余切片 | 需要先重新核对现场、编写分阶段实施计划并获批 |

方案 A 的范围、验收和权限边界以
[05-lean-developer-preview-scope.md](./05-lean-developer-preview-scope.md) 为准；全部
延期工作以 [06-deferred-work-backlog.md](./06-deferred-work-backlog.md) 为准。

**覆盖关系：**D8-A 取代 D4-A 的当前交付范围；D4-A 保留为长期路线。D5-B 的
“连续实施”收窄为 D8-A Lean 范围。S4 视觉终审和任何外部发布授权仍单独保留。

---

## 审批记录

| 决策 | 裁决 | 日期 | 备注 |
|---|---|---|---|
| D1 | **A 全量快进迁移，附加条件** | 2026-07-12 | 用户原话："全量快进迁移没问题，但是也要看下是否合理，以及是否需要重构"——落实为新增 **P1 继承实现复审**（见 01 号计划），迁移后对 44 个提交做只读架构/质量评审，Critical 发现前置到 S0，重构建议入候选清单 |
| D2 | A 唯一现场（默认推荐） | 2026-07-12 | 未异议 |
| D3 | **A bundle + 推送 origin** | 2026-07-12 | 推送 `feat_codex_phase_1` 与 `wip/g1.4b3-freeze` 到私有 origin 已获授权；不涉及公开发布 |
| D4 | **A 完整 G0–G5（当前范围已被 D8-A 收窄）** | 2026-07-12 | 保留为长期技术路线，不再表示当前版本必须一次完成全部切片 |
| D5 | **B 连续实施（当前仅适用于 D8-A Lean 范围）** | 2026-07-12 | Gate 自动验收纪律继续；S4 视觉终审与外部发布授权仍单独保留 |
| D6 | 推荐（默认） | 2026-07-12 | cc-fable-v1 为执行权威 |
| D7 | 推荐（默认） | 2026-07-12 | S0 收口后独立小提交处理 `.idea` 跟踪 |
| D8 | **A 精简 Developer Preview** | 2026-07-14 | 用户原话：“好的，采用方案A”；第一阶段范围见 05，后续续作台账见 06 |
