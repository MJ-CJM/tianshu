# S0 G1.4b3 Close Brief

## Context

- Repository: `<repo>`
- Branch: `feat_codex_phase_1`
- Implementation lineage base: `1b51bcd9`
- Review base: record the tracked `docs: add Codex v1 development handoff`
  commit with `git rev-parse HEAD` immediately before S0 resumes. Use that
  recorded SHA for S0 review packages so this documentation commit is excluded.
- The dirty tree contains approved G1.4b3 work that must be preserved.
- Read `../design/14-g1.4b3-governed-apply-brief.md` first; it is the binding behavior
  contract. Also read the S0 section and Global Constraints in
  `./01-rebaselined-execution.md`.
- Do not reset, discard, rewrite, or clean untracked user work.
- Use `env -u VIRTUAL_ENV .venv/bin/python`; do not use an ambient Python or
  `uv run`.
- Do not start the full test suite. The root controller uniquely owns broad/full
  regression.

## Deliverable

Close the existing governed-apply implementation into two coherent commits,
fixing any discovered defect test-first and writing a complete report to
`.superpowers/sdd/s0-g1.4b3-close-report.md`.

## Commit A - Governed Workspace Apply Authority

Own these production areas and their focused tests:

- `src/tianshu/models/workspace.py`
- `src/tianshu/storage/migrations.py`
- `src/tianshu/storage/workspace_repo.py`
- `src/tianshu/executor/anchored_fs.py`
- `src/tianshu/executor/workspace_apply.py`
- `src/tianshu/executor/workspace_service.py`
- `src/tianshu/executor/git_backend.py`
- `tests/services/test_workspace_governed_apply.py`
- `tests/executor/test_workspace_apply.py`
- `tests/executor/test_git_backend.py`
- `tests/executor/test_executor_workspace_lifecycle.py`
- `tests/storage/test_workspace_foundation.py`
- `tests/storage/test_migration_preserves_data.py`

Verify additive V5 migration/checksum preservation, immutable decision binding,
terminal lifecycle, source/process locking, root-anchored file operations,
symlink and TOCTOU resistance, pre/postimage ownership, Git ref/object/index
fingerprints, POSIX mode preservation, CAS, receipts, and synchronous rollback.

Run the exact focused tests for these files. If a behavior defect is found,
write a failing regression test, observe RED, implement the minimum fix, and
observe GREEN. Commit only after this focused boundary is green, using:

`feat: add governed workspace apply authority`

## Commit B - REST, Auth, CLI, and Capability Truth

Own these production areas and their focused tests:

- `src/tianshu/app.py`
- `src/tianshu/cli/main.py`
- `src/tianshu/cli/commands/workspace.py`
- `src/tianshu/gateway/auth.py`
- `src/tianshu/gateway/workspace_api.py`
- `src/tianshu/executor/capabilities.py`
- `tests/cli/test_workspace_commands.py`
- `tests/gateway/test_workspace_apply_api.py`
- `tests/gateway/test_auth.py`
- `tests/gateway/test_governance_preview.py`
- `tests/compat/test_executor_capabilities.py`
- `tests/compat/test_executor_capability_gate.py`

Verify the exact status/changes/decision/apply contracts, server-derived actor,
`workspace:apply`, request validation without secret reflection, raw-token
non-disclosure, approve `--apply-now`, status path consistency, stable CLI
JSON/table/exit behavior, and exact tested adapter capability evidence.

Run the exact focused tests for these files. Fix any discovered behavior defect
test-first. Commit only after this boundary is green, using:

`feat: expose governed workspace apply surfaces`

## Static Verification

After both commits, run:

- `env -u VIRTUAL_ENV .venv/bin/python -m compileall -q src/tianshu`
- `env -u VIRTUAL_ENV .venv/bin/python -m ruff check` on all touched Python
  files and tests
- `env -u VIRTUAL_ENV .venv/bin/python -m ruff format --check` on all touched
  Python files and tests
- the repository's configured mypy command
- the repository's configured import-linter command
- `git diff --check <recorded-S0-review-base>..HEAD`

Do not claim the full suite passed. The controller runs it after review.

## Report Contract

Write the report file with:

1. status: `DONE`, `DONE_WITH_CONCERNS`, `NEEDS_CONTEXT`, or `BLOCKED`;
2. commit SHAs and exact files per commit;
3. every command run, exit code, pass/skip/fail counts, and duration when shown;
4. RED/GREEN evidence for every new fix;
5. self-review findings and resolutions;
6. remaining risks, including any capability/product limitation;
7. confirmation that no broad/full pytest process was started.

Return only the status, commits, one-line focused-test summary, and concerns.
