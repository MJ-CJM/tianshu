# 自然语言下发敕令 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Web 端支持"一句话自然语言 → LLM 解析成敕令草稿 → 预填现有表单 → 用户确认颁发"，并精简表单默认视图。

**Architecture:** 新增只读解析端点 `POST /api/edicts/parse`（廉价 LLM，不落库），前端把草稿预填进现有 `EdictForm`，复用 `POST /api/edicts` 创建。解析核心逻辑（prompt + JSON 校验）抽成纯函数模块便于测试。

**Tech Stack:** Python 3.14 / FastAPI / pydantic / croniter；React + Ant Design + TanStack Query。

> **环境**：Python 用 `.venv/bin/python`（裸 python 缺依赖）。前端在 `web/` 下 `npx tsc --noEmit`。
> **测试约定**：功能优先；纯函数模块（Task 1）随手测，端点测试在 Task 3。

---

## 文件结构

**新建：**
- `src/tianshu/gateway/edict_parse.py` — 解析核心：prompt 常量 + `build_parse_messages` + `parse_llm_json` + `coerce_draft`
- `tests/gateway/test_edict_parse.py` — 纯函数 + 端点测试

**修改：**
- `src/tianshu/models/api.py` — `ParseEdictRequest` 模型
- `src/tianshu/gateway/api.py` — `POST /edicts/parse` 端点
- `web/src/api/edicts.ts` — `parseEdict` 封装 + 类型
- `web/src/components/edict/EdictForm.tsx` — 顶部 NL 输入框 + 预填 + 表单折叠重排
- `web/src/i18n/locales/{en,zh-modern,zh-classic}.json` — 新增 i18n key

---

## Task 1: 解析核心模块（纯函数）

**Files:**
- Create: `src/tianshu/gateway/edict_parse.py`
- Test: `tests/gateway/test_edict_parse.py`

- [ ] **Step 1: 创建 edict_parse.py**

```python
"""自然语言 → 敕令草稿 的解析核心（纯函数，便于测试）。

端点 POST /api/edicts/parse 用 LLM 把自然语言转结构化草稿，这里负责：
prompt 构造、从 LLM 输出里提取 JSON、对草稿做防御式校验/清洗。
不创建敕令，无副作用。
"""

from __future__ import annotations

import json
import re
from datetime import datetime

from croniter import croniter

_VALID_SCHEDULE = ("immediate", "once", "cron")
_VALID_PRIORITY = ("urgent", "normal", "low")

PARSE_SYSTEM_PROMPT = """你是天枢的敕令解析器。把用户的中文自然语言需求转成一个 JSON 对象，描述要下发的敕令。

只输出 JSON，不要任何解释文字、不要 markdown 代码围栏。JSON 字段（除 goal 外都可省略）：
- "goal": 字符串，敕令旨意（必填，命令式，描述要做的事）
- "title": 字符串，简短标题（可省略）
- "context": 字符串，补充背景/约束（可省略）
- "schedule": 对象，调度。形如：
    立即执行: {"type":"immediate"}
    一次性:   {"type":"once","at":"2026-05-21 14:30:00"}     // 本地北京时间，不带时区后缀
    周期:     {"type":"cron","cron":"0 18 * * *","timezone":"Asia/Shanghai"}
  用户没提到时间就省略 schedule（默认立即）。"每天X点"→cron；"明天/某具体时刻做一次"→once。
- "priority": "urgent" | "normal" | "low"（没提到就省略）
- "notes": 字符串，一句话中文说明你识别到了什么（时间、调度类型等），供用户核对。

时间一律按北京时间（Asia/Shanghai）理解。只填用户明确表达的字段，其余一律省略。
不要臆造 goal 以外的内容。"""


def build_parse_messages(text: str) -> list[dict]:
    """构造给 LLM 的消息。"""
    return [
        {"role": "system", "content": PARSE_SYSTEM_PROMPT},
        {"role": "user", "content": text},
    ]


class ParseEdictError(Exception):
    """LLM 输出无法解析为 JSON。"""


def parse_llm_json(content: str) -> dict:
    """从 LLM 输出里提取 JSON 对象。容忍 ```json 围栏与前后噪声。"""
    if not content or not content.strip():
        raise ParseEdictError("empty LLM output")
    # 去掉 ```json ... ``` 围栏
    fenced = re.search(r"```(?:json)?\s*(\{.*?\})\s*```", content, re.S)
    candidate = fenced.group(1) if fenced else None
    if candidate is None:
        # 退而求其次：截取第一个 { 到最后一个 }
        start = content.find("{")
        end = content.rfind("}")
        if start == -1 or end == -1 or end <= start:
            raise ParseEdictError("no JSON object in LLM output")
        candidate = content[start : end + 1]
    try:
        data = json.loads(candidate)
    except json.JSONDecodeError as e:
        raise ParseEdictError(f"invalid JSON: {e}") from e
    if not isinstance(data, dict):
        raise ParseEdictError("LLM JSON is not an object")
    return data


def coerce_draft(raw: dict) -> tuple[dict, str]:
    """把 LLM 的原始 dict 清洗成安全的草稿 + notes。

    丢弃非法字段而不报错；goal 缺失时返回空 goal（前端仍要求用户填）。
    时间字段不在此补时区——交给创建端点既有的 naive→Asia/Shanghai 逻辑。
    """
    draft: dict = {}

    goal = raw.get("goal")
    if isinstance(goal, str) and goal.strip():
        draft["goal"] = goal.strip()

    for key in ("title", "context"):
        v = raw.get(key)
        if isinstance(v, str) and v.strip():
            draft[key] = v.strip()

    prio = raw.get("priority")
    if isinstance(prio, str) and prio in _VALID_PRIORITY:
        draft["priority"] = prio

    sched_notes = ""
    sched = raw.get("schedule")
    if isinstance(sched, dict):
        stype = sched.get("type")
        if stype in _VALID_SCHEDULE:
            clean: dict = {"type": stype}
            if stype == "cron":
                cron = sched.get("cron")
                if isinstance(cron, str) and croniter.is_valid(cron):
                    clean["cron"] = cron
                    clean["timezone"] = (
                        sched.get("timezone")
                        if isinstance(sched.get("timezone"), str)
                        else "Asia/Shanghai"
                    )
                else:
                    clean = {}
                    sched_notes = "（未识别出合法的周期表达式，请手动填写）"
            elif stype == "once":
                at = sched.get("at")
                ok = False
                if isinstance(at, str):
                    try:
                        datetime.fromisoformat(at)
                        ok = True
                    except (TypeError, ValueError):
                        ok = False
                if ok:
                    clean["at"] = at
                else:
                    clean = {}
                    sched_notes = "（未识别出合法的执行时间，请手动填写）"
            if clean:
                draft["schedule"] = clean

    notes = raw.get("notes")
    notes_str = notes.strip() if isinstance(notes, str) and notes.strip() else ""
    notes_str = (notes_str + sched_notes).strip()
    return draft, notes_str
```

- [ ] **Step 2: 写测试**

```python
"""edict_parse 纯函数测试 + parse 端点测试。"""

from __future__ import annotations

import json

import pytest

from tianshu.gateway.edict_parse import (
    ParseEdictError,
    build_parse_messages,
    coerce_draft,
    parse_llm_json,
)


@pytest.mark.unit
def test_build_parse_messages_shape():
    msgs = build_parse_messages("每天早上9点推送天气")
    assert msgs[0]["role"] == "system"
    assert msgs[1] == {"role": "user", "content": "每天早上9点推送天气"}


@pytest.mark.unit
def test_parse_llm_json_plain():
    assert parse_llm_json('{"goal":"x"}') == {"goal": "x"}


@pytest.mark.unit
def test_parse_llm_json_fenced():
    out = parse_llm_json('```json\n{"goal":"x"}\n```')
    assert out == {"goal": "x"}


@pytest.mark.unit
def test_parse_llm_json_with_noise():
    out = parse_llm_json('好的，结果如下：{"goal":"x","priority":"low"} 完毕')
    assert out["goal"] == "x"


@pytest.mark.unit
def test_parse_llm_json_bad_raises():
    with pytest.raises(ParseEdictError):
        parse_llm_json("这不是 JSON")


@pytest.mark.unit
def test_coerce_cron():
    raw = {"goal": "推送天气", "schedule": {"type": "cron", "cron": "0 18 * * *"}, "notes": "每天18点"}
    draft, notes = coerce_draft(raw)
    assert draft["goal"] == "推送天气"
    assert draft["schedule"]["type"] == "cron"
    assert draft["schedule"]["cron"] == "0 18 * * *"
    assert draft["schedule"]["timezone"] == "Asia/Shanghai"
    assert "每天18点" in notes


@pytest.mark.unit
def test_coerce_once():
    raw = {"goal": "提醒", "schedule": {"type": "once", "at": "2026-05-21 14:30:00"}}
    draft, _ = coerce_draft(raw)
    assert draft["schedule"]["type"] == "once"
    assert draft["schedule"]["at"] == "2026-05-21 14:30:00"


@pytest.mark.unit
def test_coerce_drops_invalid_cron():
    raw = {"goal": "x", "schedule": {"type": "cron", "cron": "not a cron"}}
    draft, notes = coerce_draft(raw)
    assert "schedule" not in draft
    assert "未识别" in notes


@pytest.mark.unit
def test_coerce_drops_invalid_priority():
    raw = {"goal": "x", "priority": "超级紧急"}
    draft, _ = coerce_draft(raw)
    assert "priority" not in draft
    assert draft["goal"] == "x"
```

- [ ] **Step 3: 跑测试**

Run: `.venv/bin/python -m pytest tests/gateway/test_edict_parse.py -v`
Expected: 全部 PASS（端点测试还没加，本步只跑纯函数测试，应 9 passed）。

- [ ] **Step 4: Commit**

```bash
git add src/tianshu/gateway/edict_parse.py tests/gateway/test_edict_parse.py
git commit -m "feat: add NL-to-edict-draft parse core (prompt + json + coercion)"
```

---

## Task 2: parse 端点

**Files:**
- Modify: `src/tianshu/models/api.py`
- Modify: `src/tianshu/gateway/api.py`

- [ ] **Step 1: 加请求模型**

在 `src/tianshu/models/api.py` 的 `EdictCreateRequest` 类定义之后加：

```python
class ParseEdictRequest(BaseModel):
    text: str = Field(min_length=1)
```

- [ ] **Step 2: 加端点**

在 `src/tianshu/gateway/api.py` 中，`POST /edicts`（约 line 102）端点函数之后插入新端点。先确认文件顶部已 `from tianshu.models.api import ...`（把 `ParseEdictRequest` 加入该 import 列表）。新增：

```python
@gateway_router.post("/edicts/parse", response_model=ApiResponse)
async def parse_edict_nl(body: ParseEdictRequest, request: Request):
    """自然语言 → 敕令草稿（只读，不落库）。前端拿去预填表单。"""
    from tianshu.gateway.edict_parse import (
        ParseEdictError,
        build_parse_messages,
        coerce_draft,
        parse_llm_json,
    )

    pm = request.app.state.provider_manager
    try:
        client = pm.get_client(config_name_override="deepseek-flash")
    except Exception:
        client = pm.get_client()

    messages = build_parse_messages(body.text)
    try:
        resp = await client.chat(messages)
        raw = parse_llm_json(resp.content or "")
    except ParseEdictError as e:
        raise HTTPException(422, f"没太理解这句话，请换种说法或手动填写：{e}") from e
    except Exception as e:
        raise HTTPException(422, f"解析服务暂不可用：{type(e).__name__}") from e

    draft, notes = coerce_draft(raw)
    return ApiResponse(success=True, data={"draft": draft, "notes": notes})
```

注意：路由前缀挂在 `/api`，故全路径是 `/api/edicts/parse`。`/edicts/parse` 必须定义在 `/edicts/{edict_id}`（GET，约 line 233）之前或与之并存——FastAPI 按声明顺序匹配，`parse` 是静态段不会被 `{edict_id}` 吞掉，但为稳妥放在 `POST /edicts` 之后即可。

- [ ] **Step 3: 验证 import**

Run: `.venv/bin/python -c "from tianshu.gateway.api import gateway_router; from tianshu.models.api import ParseEdictRequest; print('ok')"`
Expected: `ok`。

- [ ] **Step 4: Commit**

```bash
git add src/tianshu/models/api.py src/tianshu/gateway/api.py
git commit -m "feat: add POST /api/edicts/parse endpoint (NL to draft)"
```

---

## Task 3: parse 端点测试

**Files:**
- Modify: `tests/gateway/test_edict_parse.py`

- [ ] **Step 1: 追加端点测试**

先 Read `tests/test_gateway.py` 顶部，复用其 `client` fixture 的构造方式（`create_app` + `lifespan` + httpx `AsyncClient` + `ASGITransport`）。在 `tests/gateway/test_edict_parse.py` 末尾追加。Mock 掉 LLM：patch `LLMClient.chat` 返回固定 JSON 文本。

```python
from unittest.mock import AsyncMock, patch

import pytest_asyncio
from httpx import ASGITransport, AsyncClient

from tianshu.app import create_app, lifespan
from tianshu.llm import LLMResponse


@pytest_asyncio.fixture
async def client():
    app = create_app()
    async with lifespan(app):
        transport = ASGITransport(app=app)
        async with AsyncClient(transport=transport, base_url="http://test") as c:
            yield c


@pytest.mark.integration
async def test_parse_endpoint_cron(client):
    fake = LLMResponse(
        content='{"goal":"每天推送天气","schedule":{"type":"cron","cron":"0 9 * * *"},"notes":"每天9点"}',
        model="x",
    )
    with patch("tianshu.llm.LLMClient.chat", new=AsyncMock(return_value=fake)):
        resp = await client.post("/api/edicts/parse", json={"text": "每天早上9点推送天气"})
    assert resp.status_code == 200
    data = resp.json()["data"]
    assert data["draft"]["goal"] == "每天推送天气"
    assert data["draft"]["schedule"]["cron"] == "0 9 * * *"
    assert "每天9点" in data["notes"]


@pytest.mark.integration
async def test_parse_endpoint_bad_json_returns_422(client):
    fake = LLMResponse(content="这根本不是 JSON", model="x")
    with patch("tianshu.llm.LLMClient.chat", new=AsyncMock(return_value=fake)):
        resp = await client.post("/api/edicts/parse", json={"text": "随便说点啥"})
    assert resp.status_code == 422
```

注意：`LLMResponse` 的构造字段以 `src/tianshu/llm.py` 实际定义为准（`content`, `model` 等）；先 Read 该 dataclass 确认必填字段，按需补齐（如 `usage`）。`config_name_override="deepseek-flash"` 在测试环境若该 provider 不存在，端点会回退 `get_client()`，patch 的是 `LLMClient.chat` 故与具体 provider 无关。

- [ ] **Step 2: 跑测试**

Run: `.venv/bin/python -m pytest tests/gateway/test_edict_parse.py -v`
Expected: 全部 PASS（纯函数 9 + 端点 2 = 11）。

- [ ] **Step 3: Commit**

```bash
git add tests/gateway/test_edict_parse.py
git commit -m "test: cover parse endpoint (cron draft + bad-json 422)"
```

---

## Task 4: 前端 API 封装 + i18n

**Files:**
- Modify: `web/src/api/edicts.ts`
- Modify: `web/src/i18n/locales/en.json`
- Modify: `web/src/i18n/locales/zh-modern.json`
- Modify: `web/src/i18n/locales/zh-classic.json`

- [ ] **Step 1: edicts.ts 加 parseEdict**

在 `web/src/api/edicts.ts` 末尾追加（`apiClient` 已在文件顶部 import）：

```typescript
export interface EdictDraft {
  goal?: string;
  title?: string;
  context?: string;
  priority?: string;
  schedule?: {
    type: "immediate" | "once" | "cron";
    cron?: string;
    at?: string;
    timezone?: string;
  };
}

export interface ParseEdictResult {
  draft: EdictDraft;
  notes: string;
}

export async function parseEdict(text: string): Promise<ParseEdictResult> {
  const { data } = await apiClient.post<{ success: boolean; data: ParseEdictResult }>(
    "/edicts/parse",
    { text },
  );
  return data.data;
}
```

- [ ] **Step 2: i18n 加 key（三个 locale 文件）**

i18n 是嵌套结构：字段在 `form.edict.field.*`，分区标题在 `form.edict.section.*`（现有 `section.advanced` / `section.longTask`）。所以：
- 把 `nlLabel/nlPlaceholder/nlButton/nlFailed` 加进 **`form.edict.field`** 子对象；
- 把 `more` 加进 **`form.edict.section`** 子对象。

先 Read 每个 locale 文件确认 `form.edict.field` 和 `form.edict.section` 两个对象的位置，在其内追加（缩进对齐同级；前一个键末尾补逗号）。

`en.json` — `form.edict.field` 内加：
```json
        "nlLabel": "Describe in one sentence",
        "nlPlaceholder": "e.g. push Shanghai weather to me every day at 9am",
        "nlButton": "Smart fill",
        "nlFailed": "Couldn't understand that — try rephrasing or fill the form manually."
```
`en.json` — `form.edict.section` 内加：
```json
        "more": "More settings"
```

`zh-modern.json` — `form.edict.field` 内加：
```json
        "nlLabel": "用一句话描述",
        "nlPlaceholder": "例如：每天早上9点给我推送上海天气",
        "nlButton": "智能填充",
        "nlFailed": "没太理解，请换种说法或手动填写下方表单。"
```
`zh-modern.json` — `form.edict.section` 内加：
```json
        "more": "更多设置"
```

`zh-classic.json` — `form.edict.field` 内加：
```json
        "nlLabel": "一句话拟旨",
        "nlPlaceholder": "例如：每日卯时为朕呈递上海天象",
        "nlButton": "智能拟旨",
        "nlFailed": "未能会意，请换一种说法或移步下方手拟。"
```
`zh-classic.json` — `form.edict.section` 内加：
```json
        "more": "更多设置"
```

- [ ] **Step 3: 前端构建校验**

Run: `cd web && npx tsc --noEmit`
Expected: 无类型错误。

- [ ] **Step 4: Commit**

```bash
git add web/src/api/edicts.ts web/src/i18n/locales/en.json web/src/i18n/locales/zh-modern.json web/src/i18n/locales/zh-classic.json
git commit -m "feat(web): add parseEdict API client + i18n keys"
```

---

## Task 5: 表单精简（调度提顶层 + 其余折叠 + 受控展开）

**Files:**
- Modify: `web/src/components/edict/EdictForm.tsx`

先 Read 整个 `EdictForm.tsx`（约 680 行）确认当前结构：当前 `schedule_type` + `cron_expr` + `schedule_at` 三块在 "advanced" Collapse 的 children 开头（约 line 334-366）；`title`(245)、执行方式块(261-315)、`context`(317-323) 在顶层；Collapse 有两个 panel：`advanced` 与 `longTask`。

- [ ] **Step 1: Collapse 改为受控（加 activeKey state）**

在组件 state 区（`const [longTaskEnabled, setLongTaskEnabled] = useState(false);` 附近）加：

```typescript
  const [activePanels, setActivePanels] = useState<string[]>([]);
```

把 `<Collapse ghost style={{ marginBottom: 24 }} items={[...]}>` 改为受控：

```typescript
      <Collapse
        ghost
        style={{ marginBottom: 24 }}
        activeKey={activePanels}
        onChange={(keys) => setActivePanels(keys as string[])}
        items={[
```

- [ ] **Step 2: 调度块提到顶层（goal 之后）**

把当前在 advanced children 开头的这三块（`schedule_type` 的 Form.Item + `scheduleType === "cron"` 块 + `scheduleType === "once"` 块，对应约 line 334-366）整体**剪切**，粘到顶层 `goal` 的 `</Form.Item>`（约 line 259）之后、`执行方式` Form.Item（line 261）之前。即顶层顺序变为：goal → 调度块 → 执行方式…。三块内容保持不变：

```tsx
      <Form.Item name="schedule_type" label={t("form.edict.field.scheduleType")}>
        <Select
          onChange={(v) => setScheduleType(v)}
          options={[
            { value: "immediate", label: t("form.edict.option.scheduleImmediate") },
            { value: "once", label: t("form.edict.option.scheduleOnce") },
            { value: "cron", label: t("form.edict.option.scheduleCron") },
          ]}
        />
      </Form.Item>
      {scheduleType === "cron" && (
        <Form.Item
          name="cron_expr"
          label={t("form.edict.field.cronExpr")}
          rules={[{ required: true, message: t("form.edict.validation.cronExprRequired") }]}
        >
          <Input placeholder={t("form.edict.placeholder.cronExpr")} />
        </Form.Item>
      )}
      {scheduleType === "once" && (
        <Form.Item
          name="schedule_at"
          label={t("form.edict.field.scheduleAt")}
          rules={[{ required: true, message: t("form.edict.validation.scheduleAtRequired") }]}
        >
          <Input placeholder={t("form.edict.placeholder.scheduleAt")} />
        </Form.Item>
      )}
```

- [ ] **Step 3: title / 执行方式 / context 移入 advanced 折叠**

把顶层的 `title` Form.Item（245-247）、整段执行方式相关块（`执行方式` Form.Item 261-274 + `assigned_persona_id`(276-288) + `planner_persona_id`(290-304) + `plan_review`(306-315)）、`context` Form.Item（317-323）整体**剪切**，粘到 advanced panel 的 `children: (<>` 开头（即原调度块被剪走后留下的位置，`priority` Form.Item 之前）。顺序建议：title → 执行方式块 → context → priority → review_policy → constraints → output_format → runtime…。这些块内容不变，只是位置进了折叠。

- [ ] **Step 4: advanced panel 标签改为"更多设置"**

把 advanced panel 的 `label: t("form.edict.section.advanced")` 改为 `label: t("form.edict.section.more")`。

- [ ] **Step 5: 前端构建校验 + 视觉自查**

Run: `cd web && npx tsc --noEmit`
Expected: 无类型错误。

启动前端 dev server，打开"颁发敕令"页，确认：默认只显示 旨意 + 调度方式（选 cron/once 时出现对应输入）；标题/执行方式/规划审批/附则/高级 runtime 都在折叠的"更多设置"里；"长任务模式"仍是第二个折叠面板。手动建一条 immediate 敕令确认提交仍正常。

- [ ] **Step 6: Commit**

```bash
git add web/src/components/edict/EdictForm.tsx
git commit -m "feat(web): surface schedule at top, fold rest into 更多设置"
```

---

## Task 6: 自然语言输入框 + 预填

**Files:**
- Modify: `web/src/components/edict/EdictForm.tsx`

- [ ] **Step 1: import + state**

确认顶部从 `antd` 引入了 `Alert`（已在 import 列表）。从 `../../api/edicts` 引入 `parseEdict`（与该文件其它 api import 同处）：

```typescript
import { parseEdict } from "../../api/edicts";
```

在 state 区加：

```typescript
  const [nlText, setNlText] = useState("");
  const [nlLoading, setNlLoading] = useState(false);
  const [nlNotes, setNlNotes] = useState<string | null>(null);
  const [nlError, setNlError] = useState<string | null>(null);
```

- [ ] **Step 2: 预填处理函数**

在 `handleFinish` 函数定义之前/之后加一个 `handleSmartFill`：

```typescript
  const handleSmartFill = async () => {
    if (!nlText.trim()) return;
    setNlLoading(true);
    setNlError(null);
    setNlNotes(null);
    try {
      const { draft, notes } = await parseEdict(nlText.trim());
      const patch: Record<string, unknown> = {};
      if (draft.goal) patch.goal = draft.goal;
      if (draft.title) patch.title = draft.title;
      if (draft.context) patch.context = draft.context;
      if (draft.priority) patch.priority = draft.priority;
      if (draft.schedule) {
        patch.schedule_type = draft.schedule.type;
        setScheduleType(draft.schedule.type);
        if (draft.schedule.cron) patch.cron_expr = draft.schedule.cron;
        if (draft.schedule.at) patch.schedule_at = draft.schedule.at;
      }
      form.setFieldsValue(patch);
      // 若草稿动了折叠区里的字段（标题/附则/优先级），展开"更多设置"
      if (draft.title || draft.context || draft.priority) {
        setActivePanels((prev) =>
          prev.includes("advanced") ? prev : [...prev, "advanced"],
        );
      }
      setNlNotes(notes || t("form.edict.field.nlLabel"));
    } catch {
      setNlError(t("form.edict.field.nlFailed"));
    } finally {
      setNlLoading(false);
    }
  };
```

注意 panel key 用的是 `"advanced"`（Task 5 未改 key，只改了 label）。

- [ ] **Step 3: NL 输入框 UI（表单顶部）**

在 `<Form ...>` 开标签之后、`title`/调度等第一个 Form.Item 之前插入：

```tsx
      <Form.Item label={t("form.edict.field.nlLabel")}>
        <Space.Compact style={{ width: "100%" }}>
          <Input.TextArea
            rows={2}
            value={nlText}
            onChange={(e) => setNlText(e.target.value)}
            placeholder={t("form.edict.field.nlPlaceholder")}
            style={{ resize: "vertical" }}
          />
          <Button type="primary" loading={nlLoading} onClick={handleSmartFill}>
            {t("form.edict.field.nlButton")}
          </Button>
        </Space.Compact>
        {nlNotes && (
          <Alert type="info" showIcon style={{ marginTop: 8 }} message={nlNotes} />
        )}
        {nlError && (
          <Alert type="warning" showIcon style={{ marginTop: 8 }} message={nlError} />
        )}
      </Form.Item>
      <Divider style={{ margin: "8px 0 16px" }} />
```

（`Button`、`Space`、`Divider`、`Input`、`Alert` 均已在该文件 antd import 中。）

- [ ] **Step 4: 前端构建校验 + 手动验证**

Run: `cd web && npx tsc --noEmit`
Expected: 无类型错误。

dev server 上：在 NL 框输入"每天早上9点给我推送上海天气"，点"智能填充"→ 旨意被填、调度方式变 cron、cron 表达式被填、提示条显示识别说明。输入一句乱码 → 显示 `nlFailed` 警告且表单仍可手填。预填后点"颁发敕令"应正常创建。

- [ ] **Step 5: Commit**

```bash
git add web/src/components/edict/EdictForm.tsx
git commit -m "feat(web): natural-language smart-fill box on edict form"
```

---

## 验收（用户手动）

1. 重启后端（新端点生效）+ 前端。
2. "颁发敕令"页默认只露 旨意 + 调度方式；其余在"更多设置"折叠里。
3. NL 框输入"每天18点推送天气"→ 智能填充 → 表单被预填、提示识别结果 → 检查无误后颁发。
4. NL 输入含一次性时间（"明天下午两点提醒我开会"）→ 调度变 once、执行时间按北京时间预填。
5. 乱码输入 → 友好提示，表单仍可手动使用。

## 自检覆盖
- spec §组件1（解析端点）→ Task 1+2+3
- spec §组件2（前端入口/预填/notes/失败回落）→ Task 4+6
- spec §组件3（表单精简默认折叠）→ Task 5
- spec §时区复用 → Task 1 coerce（once 不补时区，交创建端点既有 naive→Shanghai 修复）
- spec §错误处理（422/留空/丢弃非法）→ Task 1 coerce + Task 2 端点 + Task 6 前端
- spec §测试 → Task 1（纯函数）+ Task 3（端点）
