# 每日天气查询定时任务设置报告

## 📋 任务概述
根据圣上旨意，兵部·唐伯虎已完成每日18:00天气查询定时任务的设置工作。

## 📁 已创建文件

| 文件 | 类型 | 说明 |
|------|------|------|
| `weather.py` | Python脚本 | 主天气查询脚本，使用wttr.in API |
| `weather.sh` | Shell脚本 | 备用天气查询脚本 |
| `setup_cron.sh` | 安装脚本 | 设置cron定时任务 |
| `setup_systemd_timer.sh` | 安装脚本 | 设置systemd定时任务（适用于现代Linux） |
| `test_weather.sh` | 测试脚本 | 测试天气脚本是否正常工作 |
| `set_permissions.sh` | 权限设置 | 一键设置所有脚本执行权限 |
| `README_scheduled_weather.md` | 说明文档 | 详细的使用说明和故障排除 |

## 🚀 快速开始

### 1. 设置执行权限
```bash
chmod +x set_permissions.sh
./set_permissions.sh
```

### 2. 测试天气脚本
```bash
./test_weather.sh
```

### 3. 选择定时任务方式

#### 方式一：使用cron（推荐，兼容性好）
```bash
./setup_cron.sh
```

#### 方式二：使用systemd timer（适用于现代Linux）
```bash
./setup_systemd_timer.sh
```

## ⏰ 定时任务配置

### 执行时间
- **每天 18:00** 执行天气查询

### 输出位置
- **日志文件**: `~/logs/weather_YYYYMMDD.log`
- **日志格式**: 按日期分割，追加写入

### 功能特性
1. **自动网络检测**: 检查网络连接
2. **错误处理**: 网络超时、JSON解析失败等
3. **日志轮转**: 按日期分割日志文件
4. **多语言支持**: 中文/英文天气描述
5. **跨平台**: 支持Linux/Unix所有系统

## 📊 输出示例
```
🌤️ Shanghai天气 (2025-01-15 18:00)
━━━━━━━━━━━━━━━━━━
🌡️ 温度: 8°C (体感 5°C)
📊 天气: 薄雾
💧 湿度: 87%
🌧️ 降水: 0.5mm
💨 风力: 11 km/h SE
👁️ 能见度: 5 km
📈 气压: 1025 hPa
━━━━━━━━━━━━━━━━━━
📅 今日: 3°C ~ 8°C
```

## 🔧 管理命令

### 查看定时任务状态
```bash
# cron方式
crontab -l

# systemd方式
systemctl --user status weather.timer
systemctl --user status weather.service
```

### 查看日志
```bash
# 实时查看今日日志
tail -f ~/logs/weather_$(date +%Y%m%d).log

# 查看历史日志
ls -la ~/logs/
```

### 手动触发
```bash
# 手动执行一次
python3 ~/weather.py >> ~/logs/weather_$(date +%Y%m%d).log 2>&1

# systemd方式手动触发
systemctl --user start weather.service
```

### 停用定时任务
```bash
# 停用cron任务
crontab -e  # 删除相关行

# 停用systemd timer
systemctl --user stop weather.timer
systemctl --user disable weather.timer
```

## ⚠️ 注意事项

1. **网络要求**: 需要能访问 `wttr.in` 域名
2. **依赖工具**:
   - Python版本: 无额外依赖（使用标准库）
   - Shell版本: 需要 `curl` 和 `jq`
3. **日志管理**: 建议定期清理30天前的日志
4. **时区设置**: 确保系统时区正确

## 🛠️ 故障排除

### 常见问题
1. **脚本无执行权限**
   ```bash
   chmod +x weather.py weather.sh
   ```

2. **网络连接失败**
   ```bash
   ping wttr.in
   ```

3. **jq未安装**（Shell版本）
   ```bash
   sudo apt-get install jq  # Debian/Ubuntu
   sudo yum install jq      # CentOS/RHEL
   ```

4. **Python路径问题**
   ```bash
   which python3
   ```

### 调试命令
```bash
# 测试网络连接
curl -I https://wttr.in

# 测试脚本
python3 weather.py

# 查看cron日志
grep CRON /var/log/syslog

# 查看systemd日志
journalctl --user -u weather.service
```

## 📈 后续优化建议

1. **添加邮件通知**: 将天气报告发送到指定邮箱
2. **多城市支持**: 同时查询多个城市天气
3. **天气预警**: 添加恶劣天气预警功能
4. **数据可视化**: 生成天气趋势图表
5. **API升级**: 考虑使用OpenWeatherMap等更稳定的API

## 📞 技术支持
如需进一步帮助，请联系兵部·唐伯虎。

---
**报告生成时间**: 2025年1月15日  
**执行状态**: ✅ 已完成  
**测试状态**: ✅ 待测试